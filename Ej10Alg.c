#include <math.h>
#include <stdlib.h>
#include <stdio.h>

int main(int argc, char const *argv[]) {
  double P == atof(argv[1];
  double t == atof(argv[2];
  double r == atof(argv[3];
  printf("%.2f\n", P * exp (r * t)); 
  return 0; 
}
